{
  "name": "gnawa-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "backend": "npm run dev --workspace=backend",
    "frontend": "npm start --workspace=frontend",
    "seed": "npm run seed --workspace=backend",
    "install:all": "npm install",
    "install:backend": "npm install --workspace=backend",
    "install:mobile": "npm install --workspace=frontend"
  }
}
